#define NULL         ((void *)0)

#define max(a,b)        (((a) > (b)) ? (a) : (b))
#define min(a,b)        (((a) < (b)) ? (a) : (b))

#include <stdint.h>

long atol(const char *string);
int atoi(const char *string);
